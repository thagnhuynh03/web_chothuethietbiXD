@model IEnumerable<WebChoThueThietBiXD.Models.HinhAnhThietBi>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.hinhAnh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThietBi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.hinhAnh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ThietBi.tenThietBi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.maHinhAnh">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.maHinhAnh">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.maHinhAnh">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
